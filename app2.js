/*TOÁN TỬ SO SÁNH
// 1. if(đk){

}else{

}
VÍ DỤ1 let a = 1;
let b = 2;
if (a>b) {
     console.log( `Điều kiện đúng!`);
} 
else {
     console.log( `Điều kiện sai! `);
}
VÍ DỤ 2: var a = true;

console.log(a);*/
/**
 * Bài 1: Cho người dùng nhập tuổi
 * nếu < 18 cho phép đi học
 * nếu > 18 thì k đươc phép đi học
 */
// let age = Number(prompt(" Mời nhập tuổi:"));
// if(age < 18) {
//      console.log(`Cho phép đi học`)
// } else { 
//      console.log(` Không cho phép đi học`)
// }
/**
 * Bài 2: Cho người dùng nhập 1 số bất kìtừ 1 fa-rotat
 * nếu < số default của hệ thống, log ra số nhỏ hơn
 * nếu = thì log ra bằng
 * trường hợp còn lại, log ra lớn hơn
 * while thường được sử dụng để lặp đi lặp lại
 *  một khối lệnh không biết trước số lần lặp.
 */
//let a = Number(prompt("Xin mời nhập số"))
// let defaultNumber = Math.floor(Math.random() * 10)
// console.log( "default number:" + defaulNumber)
// let isloop = true;
// while (isloop) {
//      let inputNumber = Number(promt(" Xin mời nhập số"))
// if( inputNumber < defaultNumber) {
//      alert(" Bạn đã nhập số nhỏ hơn số yêu cầu")
// } else (inputNumber > defaultNumber ) {
//      alert(" Bạn đã nhập số lơn hơn số yêu cầu")
// } else {
//      alert( " Chính xác")
//      isloop = false
// }
// }

// let i = 0
// while(i < 5){
//      console.log(i);
//      i++

